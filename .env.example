# Copy to .env for local dev. Never commit the real .env.
PORT=3000
SESSION_SECRET=change-me
# Optional SMTP for real confirmation emails (leave blank to log emails to console)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
